#include "main.h"
/**
 * mul - multiplies 2 integers.
 * @a is first parameter.
 * @b is second parameter.
 * Return: 0 (Success)
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
